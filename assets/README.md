# Default background music

Add your background music file here and name it exactly:

`background_music.mp3`

Every Preview Render and Final Render will loop this track for the full video and mix it under the narration at 16 percent volume. You can replace it per project in Settings & Details. Leave the file out to render without default background music.
